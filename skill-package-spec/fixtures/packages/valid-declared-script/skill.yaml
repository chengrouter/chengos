schema_version: "1"

name: valid-declared-script
display_name: Valid Declared Script
description: Valid Package v1 fixture with one declared helper shell script.
version: 0.3.2
category: tools
tags:
  - fixture
  - script

backend:
  type: reference

scripts:
  - path: scripts/format-report.sh
    interpreter: bash
    purpose: helper
    description: Formats a report file into Markdown. Documentation helper only.

credentials:
  required: []

permissions:
  read_only: true
  allowed_actions: []
  denied_actions: []

normalization:
  status: ready
  confidence: 1.0
  warnings: []
