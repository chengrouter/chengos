# Valid Declared Script

Valid Package v1 fixture containing one declared helper script. Verifies that
a text script listed in both `manifest.scripts[]` and `skill.yaml scripts:`
passes validation (elevated risk, not invalid).
