# Valid Minimal

Minimal valid Package v1 fixture. Use this skill's instructions section to
verify that a plain Markdown + YAML package with no workflow, references, or
scripts passes both Registry and ChengFlow validation.
