# Production deployment environment for ChengOS

# Public image to pull on the VPS
CHENGOS_IMAGE=chengos/chengos:latest

# Exposed application port on the VPS
APP_PORT=3000
PORT=3000

# Database and Redis passwords for the infrastructure containers
POSTGRES_PASSWORD=change_this_postgres_password
REDIS_PASSWORD=change_this_redis_password

# Application connection strings
DATABASE_URL=postgres://tianai_db:change_this_postgres_password@postgres:5432/master_router
REDIS_URL=redis://:change_this_redis_password@redis:6379

# Connection pool tuning
DATABASE_MAX_CONNECTIONS=10
DATABASE_MIN_CONNECTIONS=1
DATABASE_CONNECTION_TIMEOUT=30
REDIS_MAX_CONNECTIONS=10
REDIS_MIN_CONNECTIONS=1
REDIS_CONNECTION_TIMEOUT=30

# Agent Memory connection details
DB_HOST=postgres
DB_PORT=5432
DB_NAME=master_router
DB_USER=tianai_db
DB_PASSWORD=change_this_postgres_password
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change_this_redis_password
# REDIS_DB=0

# Required secrets
# Generate with: openssl rand -hex 32
CREDENTIAL_MASTER_KEY_1=replace_with_64_hex_chars
JWT_SECRET=replace_with_64_hex_chars

# Runtime
RUN_MIGRATIONS=true
RUST_LOG=info
RUST_BACKTRACE=1
DEBUG_ENV_LOADED=false

# Scheduler
SCHEDULER_ENABLED=true
SCHEDULER_NODE_ID=vps-node-1
SCHEDULER_HOT_WINDOW_SECS=60
SCHEDULER_WARM_WINDOW_SECS=300
SCHEDULER_SWEEPER_INTERVAL_SECS=30
SCHEDULER_LOCK_TIMEOUT_SECS=60
SCHEDULER_MAX_CONCURRENCY=8

# CORS
CORS_PERMISSIVE=false
CORS_ALLOWED_ORIGINS=https://your-frontend.example.com

# MCP
MCP_ENABLED=false
# MCP_SERVER_TOKEN=replace_with_a_long_random_token
# MCP_GATEWAY_PORT_START=9100

# OAuth2
# OAUTH2_GOOGLE_CLIENT_ID=
# OAUTH2_GOOGLE_CLIENT_SECRET=
# OAUTH2_GITHUB_CLIENT_ID=
# OAUTH2_GITHUB_CLIENT_SECRET=
# OAUTH2_MICROSOFT_CLIENT_ID=
# OAUTH2_MICROSOFT_CLIENT_SECRET=
# OAUTH2_REDIRECT_URI=https://your-api.example.com/api/v1/oauth2/callback

# Optional mail settings
EMAIL_ENABLED=false
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=mailer@example.com
# SMTP_PASSWORD=change_me
# SMTP_FROM=mailer@example.com
# PASSWORD_RESET_BASE_URL=https://your-frontend.example.com/reset-password
